# 项目配置文档

**⚠️ 安全提示：** 本文档包含敏感信息，请勿提交到公共代码仓库。

---

## 1. 设计工具

### Figma API
```
Token: [请在本地配置文件中设置，不要提交到仓库]
```

---

## 2. 数据库

### PostgreSQL
```
版本: 17.5

开发环境:
  Host: localhost
  Port: 5432
  Database: tennis_db
  Username: postgres
  Password: [请在本地.env文件中配置，不要提交到仓库]

生产环境:
  Host: [待配置]
  Port: 5432
  Database: tennis_social_prod
  Username: [待配置]
  Password: [待配置]
```

---

## 3. 云服务（阿里云）

```
Access Key ID: [待配置]
Access Key Secret: [待配置]
Region: cn-hangzhou

OSS:
  Bucket: [待配置]
  Endpoint: [待配置]

ECS:
  实例 ID: [待配置]
  公网 IP: [待配置]
```

---

## 4. 第三方服务

### 短信服务（阿里云）
```
Access Key ID: [待配置]
Access Key Secret: [待配置]
签名名称: [待配置]
```

### 地图服务（高德）
```
Web Key: [待配置]
Android Key: [待配置]
iOS Key: [待配置]
```

### 邮件服务
```
SMTP Host: [待配置]
SMTP Port: 587
发件邮箱: [待配置]
密码: [待配置]
```

---

## 5. 后端配置

### Python 开发环境
```
Python 版本: 3.11.14
虚拟环境: develop (conda 环境)
环境路径: C:\Users\18113\.conda\envs\develop
```

### FastAPI
```
版本: 0.104.1
APP_NAME: Tennis Social Platform API
PORT: 8000
DEBUG: True (生产环境 False)

JWT:
  SECRET_KEY: [请在本地.env文件中配置，不要提交到仓库]
  ALGORITHM: HS256
  ACCESS_TOKEN_EXPIRE: 30分钟
```

### Python 后端依赖
```
Web 框架:
  fastapi: 0.104.1
  uvicorn: 0.24.0

数据库相关:
  sqlalchemy: 2.0.23        # ORM
  alembic: 1.12.1          # 数据库迁移
  psycopg2-binary: 2.9.9   # PostgreSQL 驱动

数据验证:
  pydantic: 2.5.0
  pydantic-settings: 2.1.0

认证与安全:
  python-jose[cryptography]: 3.3.0  # JWT
  passlib[bcrypt]: 1.7.4            # 密码加密

其他工具:
  python-multipart: 0.0.6   # 文件上传
  aiofiles: 23.2.1           # 异步文件操作
  pillow: 10.1.0             # 图像处理
  python-dotenv: 1.2.1       # 环境变量
  httpx: 0.25.2              # HTTP 客户端
```

---

## 6. 前端配置

### Flutter
```
版本: 3.38.0-1.0.pre-313
Channel: master
Framework: revision 376a0b25d8
Dart 版本: 3.11.0
DevTools: 2.51.0

API 端点:
  开发: http://localhost:8000/api
  生产: https://api.example.com/api

Android:
  applicationId: [待配置]
  minSdkVersion: 21

iOS:
  Bundle Identifier: [待配置]
  Minimum Version: 12.0
```

### Flutter 前端依赖
```
核心框架:
  flutter_riverpod: ^2.4.9      # 状态管理
  go_router: ^13.0.0            # 路由导航

网络与存储:
  dio: ^5.4.0                   # 网络请求
  shared_preferences: ^2.2.2     # 本地存储

UI 相关:
  flutter_screenutil: ^5.9.0    # 屏幕适配
  Material Design 3              # UI 设计规范

数据序列化:
  json_annotation: ^4.8.1
  json_serializable: ^6.7.1     # 代码生成

图片处理:
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1

其他工具:
  intl: ^0.19.0                 # 国际化
  logger: ^2.0.2                # 日志
```

---

## 7. 环境变量（.env）

```bash
# 应用
DEBUG=True
SECRET_KEY=[请在本地.env文件中配置，不要提交到仓库]

# 数据库
DATABASE_URL=[请在本地.env文件中配置，格式: postgresql://username:password@host:port/database]

# 阿里云
ALIYUN_ACCESS_KEY_ID=[待配置]
ALIYUN_ACCESS_KEY_SECRET=[待配置]
ALIYUN_OSS_BUCKET=[待配置]

# JWT
JWT_SECRET_KEY=[请在本地.env文件中配置，不要提交到仓库]
JWT_ALGORITHM=HS256
```

---

## 更新记录

| 日期 | 更新内容 |
|------|---------|
| 2025-11-06 | 初始创建，添加 Figma API |
| 2025-11-06 | 添加技术栈版本信息（Flutter、Python、FastAPI） |
| 2025-11-06 | 添加所有后端和前端依赖包版本信息 |
| 2025-11-06 | 配置环境变量模板（敏感信息已移除） |
